# 成果物一覧

# 1. システム概要

本成果物は、

「交通データ分析による熊本県の渋滞可視化と改善提案」

のPoC（Proof of Concept）として作成した交通分析システムです。

主な目的は以下です。

* 熊本県の交通課題可視化
* ETL基盤試作
* Dockerによる再現性確保
* 交通データ分析
* 渋滞改善検討

---

# 2. 成果物一覧

| 分類     | 内容                       |
| ------ | ------------------------ |
| 社内論文   | 交通データ分析による熊本県の渋滞可視化と改善提案 |
| README | システム概要・起動方法              |
| ETL処理  | CSV取込・データ加工              |
| DB     | PostgreSQL               |
| API    | Spring Boot REST API     |
| Docker | 環境再現                     |
| CSV    | サンプル交通データ                |

---

# 3. ディレクトリ構成

```text id="u8jjlwm"
kumamoto-traffic-etl/
├── README.md
├── docker-compose.yml
├── .env
├── sample-data/
│   └── traffic_data.csv
├── sql/
│   └── schema.sql
├── backend/
│   ├── Dockerfile
│   ├── pom.xml
│   └── src/
│       └── main/
│           ├── java/com/example/traffic/
│           │   ├── controller/
│           │   ├── entity/
│           │   ├── repository/
│           │   ├── service/
│           │   └── etl/
│           └── resources/
│               └── application.yml
└── docs/
    ├── architecture.md
    ├── api-spec.md
    └── images/
```

kumamoto-traffic-etl/
├── README.md
├── 成果物一覧.md
├── 社内論文.md
├── docker-compose.yml
├── sample-data/
├── sql/
├── backend/
└── docs/

---

# 4. docker-compose.yml

```yaml id="jnnncm"
version: '3.9'

services:

  postgres:
    image: postgres:16
    container_name: traffic-postgres

    environment:
      POSTGRES_DB: trafficdb
      POSTGRES_USER: trafficuser
      POSTGRES_PASSWORD: trafficpass

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d

  backend:
    build: ./backend
    container_name: traffic-backend

    depends_on:
      - postgres

    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/trafficdb
      SPRING_DATASOURCE_USERNAME: trafficuser
      SPRING_DATASOURCE_PASSWORD: trafficpass

    ports:
      - "8080:8080"

    volumes:
      - ./sample-data:/app/sample-data

volumes:
  postgres-data:
```

---

# 5. Dockerfile

## backend/Dockerfile

```dockerfile id="77x5ei"
FROM eclipse-temurin:21-jdk AS build

WORKDIR /app

COPY . .

RUN chmod +x mvnw
RUN ./mvnw clean package -DskipTests

FROM eclipse-temurin:21-jre

WORKDIR /app

COPY --from=build /app/target/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
```

---

# 6. schema.sql

```sql id="f3ewf4"
CREATE TABLE traffic_data (
    id SERIAL PRIMARY KEY,
    road_name VARCHAR(255),
    observation_time TIMESTAMP,
    average_speed NUMERIC(5,2),
    traffic_volume INTEGER,
    congestion_level VARCHAR(20)
);
```

---

# 7. サンプルCSV

## sample-data/traffic_data.csv

```csv id="jlwmk0"
road_name,observation_time,average_speed,traffic_volume
国道57号,2026-05-01 07:00:00,18.5,1200
国道57号,2026-05-01 08:00:00,12.3,1800
国道57号,2026-05-01 09:00:00,21.0,900
国道3号,2026-05-01 07:00:00,25.0,950
国道3号,2026-05-01 08:00:00,14.5,1600
国道3号,2026-05-01 09:00:00,27.1,800
流通団地入口,2026-05-01 17:00:00,11.2,2100
流通団地入口,2026-05-01 18:00:00,9.8,2400
高下西町,2026-05-01 17:00:00,15.1,1900
高下西町,2026-05-01 18:00:00,13.4,2200
```

---

# 8. Entity

## TrafficData.java

```java id="e19t6p"
package com.example.traffic.entity;

import jakarta.persistence.*;
import lombok.Getter;
import lombok.Setter;

import java.time.LocalDateTime;

@Entity
@Getter
@Setter
public class TrafficData {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String roadName;

    private LocalDateTime observationTime;

    private Double averageSpeed;

    private Integer trafficVolume;

    private String congestionLevel;
}
```

---

# 9. Repository

## TrafficRepository.java

```java id="c6djgo"
package com.example.traffic.repository;

import com.example.traffic.entity.TrafficData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrafficRepository
        extends JpaRepository<TrafficData, Long> {
}
```

---

# 10. ETL処理

## CsvImportService.java

```java id="c3x2cf"
package com.example.traffic.etl;

import com.example.traffic.entity.TrafficData;
import com.example.traffic.repository.TrafficRepository;
import com.opencsv.CSVReader;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

import java.io.FileReader;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

@Service
@RequiredArgsConstructor
public class CsvImportService {

    private final TrafficRepository repository;

    public void importCsv(String path) throws Exception {

        CSVReader reader =
                new CSVReader(new FileReader(path));

        String[] line;

        reader.readNext();

        while ((line = reader.readNext()) != null) {

            TrafficData data = new TrafficData();

            data.setRoadName(line[0]);

            data.setObservationTime(
                    LocalDateTime.parse(
                            line[1],
                            DateTimeFormatter.ofPattern(
                                    "yyyy-MM-dd HH:mm:ss"
                            )
                    )
            );

            double speed =
                    Double.parseDouble(line[2]);

            data.setAverageSpeed(speed);

            data.setTrafficVolume(
                    Integer.parseInt(line[3])
            );

            if (speed < 15) {
                data.setCongestionLevel("HIGH");
            } else if (speed < 25) {
                data.setCongestionLevel("MEDIUM");
            } else {
                data.setCongestionLevel("LOW");
            }

            repository.save(data);
        }
    }
}
```

---

# 11. API

## TrafficController.java

```java id="e0zpcq"
package com.example.traffic.controller;

import com.example.traffic.entity.TrafficData;
import com.example.traffic.repository.TrafficRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/traffic")
@RequiredArgsConstructor
public class TrafficController {

    private final TrafficRepository repository;

    @GetMapping
    public List<TrafficData> getAll() {
        return repository.findAll();
    }
}
```

---

# 12. application.yml

```yaml id="0w4v7p"
spring:

  datasource:
    url: jdbc:postgresql://postgres:5432/trafficdb
    username: trafficuser
    password: trafficpass

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
```

---

# 13. 起動方法

## 1. リポジトリ取得

```bash id="5x4lb6"
git clone https://github.com/yourname/kumamoto-traffic-etl.git
```

---

## 2. 起動

```bash id="u8w7t6"
docker compose up --build
```

---

## 3. API確認

```bash id="pd7n3o"
curl http://localhost:8080/api/traffic
```

---

# 14. 今後の拡張

* Chart.jsによるグラフ表示
* 地図表示
* ヒートマップ
* AI渋滞予測
* OpenData連携
* リアルタイム分析
* 自転車ナビゲーション

---

# 15. 本成果物の位置付け

本成果物は完成品ではなく、

* ETL
* Spring Boot
* PostgreSQL
* Docker
* データ分析

を用いたPoC（概念実証）として位置付けている。

社内論文と組み合わせることで、

「社会課題へIT技術をどのように適用できるか」

を示すことを目的としている。

